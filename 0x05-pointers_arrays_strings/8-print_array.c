#include "main.h"
#include <stdio.h>
/**
 * print_array - prints n element of an array of integers
 * Owned by salmane
 * @a: int to check
 * @n: int to check
 * Return: 0 is success
 */
void print_array(int *a, int n)
{
	int z;

	for (z = 0; x < n; z++)
		if (z != n - 1)
		printf("%d, ", a[z]);
		else
			printf("%d", a[z]);
printf("\n");
}
